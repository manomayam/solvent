import { SpectrumElement } from '@spectrum-web-components/base';

/**
 * @summary Base class for the project elements.
 */
export default class SNBase extends SpectrumElement {
  /** @type {import("lit").PropertyDeclarations} */
  static properties = {};

  constructor() {
    super();
  }
}
